在spring给对象赋值。


di：依赖注入，表示创建对象，赋值。

di实现方法有两种：
1.在spring配置文件beans.xml中使用标签和属性完成，叫做基于xml的di实现。
2.使用spring中的注解，完成属性的赋值，叫做基于注解的di实现


di的语法分类：
1.set注入：spring调用类的set方法，在set方法中实现属性的赋值

2.构造注入，spring调用类的有参构造方法，在构造方法中完成赋值